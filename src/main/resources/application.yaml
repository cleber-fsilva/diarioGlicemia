spring:
  application:
  name: diarioGlicemia

  datasource:
    url: postgres.railway.internal
    #    docker run --name database -p 2222:5432 -e POSTGRES_PASSWORD=postgres -d postgres
    username: postgres
    password: bIYGXLflSIbCDOpDdEwHsEDtLWGcyRiO
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true

diarioGlicemia:
  security:
    secret: "palavra-secreta"